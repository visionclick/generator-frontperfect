{
	"name": "<%= _.slugify(appname) %>",
	"version": "0.0.0",
	"dependencies": { <%
		if (compassBootstrap) { %>
			"bootstrap-sass": "~3.3.4", <%
		} %> <%
		if (groundworkCSS) { %>
			"groundwork": "^2.0.2", <%
		} %> <%
		if (includeRequireJS) { %>
			"requirejs": "^2.1.8", <%
		} %> <%
		if (includeModernizr) { %>
			"modernizr": "^2.6.2", <%
		} %>
		"jquery": "^1.10.2",
		"lt-ie-9": "~0.1.0"
	},
	"devDependencies": {}
}
